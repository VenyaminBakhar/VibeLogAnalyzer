server:
  port: 8080

spring:
  application:
    name: log-analyzer
  
  # H2 Database for development (ClickHouse alternative)
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
    driver-class-name: org.h2.Driver
    
  h2:
    console:
      enabled: true
      path: /h2-console
      
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true


# Logging
logging:
  level:
    com.loganalyzer: DEBUG
    org.springframework.web: DEBUG
    com.clickhouse: DEBUG